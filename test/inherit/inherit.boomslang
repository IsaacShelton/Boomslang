
class Animal
	Integer legs

class Dog Animal
	String name
	String breed
	
	def new(String name, String breed)
		self.name = name
		self.breed = breed
		legs = 4
	
	def output()
		("The dog '" + name + "' is a " + breed).output()

def main()
	Dog ruff = new Dog("Ruff", "Mutt")
	ruff.output()
