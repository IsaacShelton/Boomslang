
def main()
	Array<String> strings
	strings.resize(3)
	strings.get(0) = "Hello World"
	strings.get(1) = "Hi World"
	strings.get(2) = "Greetings World"
	
	strings.get(0).output()
	strings.get(1).output()
	strings.get(2).output()