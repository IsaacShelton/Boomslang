
# NOTE: This is outdated and function pointers are still experimental and unsupported at the moment

def greet(String message)
	message.output()

def main()
	function^(String)->void action = greet
	action("Hi World")